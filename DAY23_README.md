# Day 23

Home and Company Profile screens implemented. Automatic Streamlit pages navigation is disabled; the requested custom sidebar is used.

Run from the project root:
`streamlit run src/dashboard/app.py`

Install if needed:
`pip install streamlit pandas plotly`
